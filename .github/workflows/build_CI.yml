name: Version latest with Certificates

on:
  push:
    branches: [ main, caliptra-main-14 ]
  pull_request:
    branches: [ main, caliptra-main-14 ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  BUILD_TYPE: Release

jobs:
  format_check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        # If any *.c *.h *.md file(except:libspdm) have Tab, the check will fail.
      - name: Check code format
        run: |
          if grep -rn "	" * --include=*.c --include=*.h --include=*.md;
          then exit 1
          fi

  gcc_mbedtls_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -E env CFLAGS="-DLIBSPDM_DEBUG_LIBSPDM_ASSERT_CONFIG=3" cmake -DARCH=x64 -DTOOLCHAIN=GCC -DTARGET=Release -DCRYPTO=mbedtls ..
          make copy_sample_key
          make -j`nproc`
      - name: Emu_Test
        run: |
          cd build/bin

          test_port=2323
          TCPListeningnum=`netstat -an | grep ":$test_port " | awk '/^tcp.*/ && $NF == "LISTEN" {print $0}' | wc -l`
          UDPListeningnum=`netstat -an | grep ":$test_port " | awk '/^udp.*/ && $NF == "0.0.0.0:*" {print $0}' | wc -l`
          Listeningnum=$((TCPListeningnum + UDPListeningnum))
          if [ $Listeningnum -eq 0 ]; then
              echo "port is not used"
          else
              echo "port is used"
          fi
          ./spdm_responder_emu  &
          sleep 5s
          ./spdm_requester_emu >requester.log

          sleep 10s

      - name: Responder_validator_Test
        run: |
          cd build/bin
          # Disable mutual auth: the validator's KEY_EXCHANGE_RSP cases do not expect
          # the responder to request it, but spdm_responder_emu asks for it by default.
          ./spdm_responder_emu --basic_mut_auth NO --mut_auth NO &
          sleep 5s
          ./spdm_device_validator_sample
          # spdm_device_validator_sample exits 0 even when assertions fail; the
          # verdict is only recorded in test.log, so check it explicitly.
          if grep -E 'test suite \(.*\) - pass: [0-9]+, fail: [1-9][0-9]*' test.log; then
            echo "SPDM responder conformance suite reported failures"
            exit 1
          fi
      - name: Test_RECORD_TRANSCRIPT_DATA_consistent
        # open LIBSPDM_RECORD_TRANSCRIPT_DATA_SUPPORT flag
        # mkdir build1 folder, gen spdm_requester_emu.exe and spdm_responder_emu.exe
        # check the log size and end of log to ensure that hashes/signatures over transcripts are consistent
        run: |
          sed -i '17a add_definitions(-DLIBSPDM_RECORD_TRANSCRIPT_DATA_SUPPORT=1)' CMakeLists.txt

          mkdir build1
          cd build1
          cmake -E env CFLAGS="-DLIBSPDM_DEBUG_LIBSPDM_ASSERT_CONFIG=3" cmake -DARCH=x64 -DTOOLCHAIN=GCC -DTARGET=Release -DCRYPTO=mbedtls ..
          make copy_sample_key
          make -j`nproc`

          cd bin
          echo "requester (DATA_SUPPORT = 0) <=> responder (DATA_SUPPORT = 1)"
          ./spdm_responder_emu &
          sleep 5s
          ./../../build/bin/spdm_requester_emu >requester.log

          sleep 10s
          echo "requester (DATA_SUPPORT = 1) <=> responder (DATA_SUPPORT = 0)"
          ./../../build/bin/spdm_responder_emu &
          sleep 5s
          ./spdm_requester_emu >requester.log
      - name: Emu_PCIDOE_Test
        run: |
          cd build/bin
          ./spdm_responder_emu --trans PCI_DOE &
          sleep 5s
          ./spdm_requester_emu --trans PCI_DOE
          sleep 10s

  gcc_openssl_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -E env CFLAGS="-DLIBSPDM_DEBUG_LIBSPDM_ASSERT_CONFIG=3" cmake -DARCH=x64 -DTOOLCHAIN=GCC -DTARGET=Release -DCRYPTO=openssl ..
          make copy_sample_key
          make -j`nproc`
      - name: Validator_group_selection_Test
        run: |
          cd build/bin
          ./spdm_device_validator_sample --test-groups VERSION --help |
            grep -q "Selected test groups: VERSION"
          if ./spdm_device_validator_sample --test-groups VERSION --test-groups CAPABILITIES; then
            echo "Duplicate --test-groups unexpectedly succeeded"
            exit 1
          fi
          if ./spdm_device_validator_sample --test-groups VERSION,UNKNOWN; then
            echo "Unknown validator group unexpectedly succeeded"
            exit 1
          fi
      - name: Emu_Test
        run: |
          cd build/bin

          test_port=2323
          TCPListeningnum=`netstat -an | grep ":$test_port " | awk '/^tcp.*/ && $NF == "LISTEN" {print $0}' | wc -l`
          UDPListeningnum=`netstat -an | grep ":$test_port " | awk '/^udp.*/ && $NF == "0.0.0.0:*" {print $0}' | wc -l`
          Listeningnum=$((TCPListeningnum + UDPListeningnum))
          if [ $Listeningnum -eq 0 ]; then
              echo "port is not used"
          else
              echo "port is used"
          fi
          ./spdm_responder_emu  &
          sleep 5s
          ./spdm_requester_emu >requester.log
          sleep 10s

      - name: Responder_validator_Test
        run: |
          cd build/bin
          # Disable mutual auth: the validator's KEY_EXCHANGE_RSP cases do not expect
          # the responder to request it, but spdm_responder_emu asks for it by default.
          ./spdm_responder_emu --basic_mut_auth NO --mut_auth NO &
          sleep 5s
          ./spdm_device_validator_sample
          # spdm_device_validator_sample exits 0 even when assertions fail; the
          # verdict is only recorded in test.log, so check it explicitly.
          if grep -E 'test suite \(.*\) - pass: [0-9]+, fail: [1-9][0-9]*' test.log; then
            echo "SPDM responder conformance suite reported failures"
            exit 1
          fi
      - name: Test_RECORD_TRANSCRIPT_DATA_consistent
        # open LIBSPDM_RECORD_TRANSCRIPT_DATA_SUPPORT flag
        # mkdir build1 folder, gen spdm_requester_emu.exe and spdm_responder_emu.exe
        # check the log size and the end of log to ensure that hashes/signatures over transcripts are consistent
        run: |
          sed -i '17a add_definitions(-DLIBSPDM_RECORD_TRANSCRIPT_DATA_SUPPORT=1)' CMakeLists.txt

          mkdir build1
          cd build1
          cmake -DARCH=x64 -DTOOLCHAIN=GCC -DTARGET=Release -DCRYPTO=openssl ..
          make copy_sample_key
          make -j`nproc`

          cd bin
          echo "requester (DATA_SUPPORT = 0) <=> responder (DATA_SUPPORT = 1)"
          ./spdm_responder_emu &
          sleep 5s
          ./../../build/bin/spdm_requester_emu >requester.log

          sleep 10s
          echo "requester (DATA_SUPPORT = 1) <=> responder (DATA_SUPPORT = 0)"
          ./../../build/bin/spdm_responder_emu &
          sleep 5s
          ./spdm_requester_emu >requester.log
      - name: Emu_PCIDOE_Test
        run: |
          cd build/bin
          ./spdm_responder_emu --trans PCI_DOE &
          sleep 5s
          ./spdm_requester_emu --trans PCI_DOE
          sleep 10s


  VS2019_mbedtls_build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
        #ilammy/msvc-dev-cmd@v1 is GitHub Action for configuring Developer Command Prompt for Microsoft Visual Studio on Windows.
        #This sets up the environment for compiling C/C++ code from command line.
      - name: Add msbuild to PATH
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -E env CFLAGS="-DLIBSPDM_DEBUG_LIBSPDM_ASSERT_CONFIG=3" cmake -G"NMake Makefiles" -DARCH=x64 -DTOOLCHAIN=VS2019 -DTARGET=Release -DCRYPTO=mbedtls ..
          nmake copy_sample_key
          nmake
      - name: Emu_Test
        run: |
          cd build/bin
          ./spdm_responder_emu &
          sleep 5s
          ./spdm_requester_emu
          sleep 10s
      - name: Responder_validator_Test
        run: |
          cd build/bin
          ./spdm_responder_emu &
          sleep 5s
          ./spdm_device_validator_sample
      - name: Emu_PCIDOE_Test
        run: |
          cd build/bin
          ./spdm_responder_emu --trans PCI_DOE &
          sleep 5s
          ./spdm_requester_emu --trans PCI_DOE
          sleep 10s

  VS2019_openssl_build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
        #ilammy/msvc-dev-cmd@v1 is GitHub Action for configuring Developer Command Prompt for Microsoft Visual Studio on Windows.
        #This sets up the environment for compiling C/C++ code from command line.
      - name: Add msbuild to PATH
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Build
        run: |
          mkdir build
          cd build
          cmake -E env CFLAGS="-DLIBSPDM_DEBUG_LIBSPDM_ASSERT_CONFIG=3" cmake -G"NMake Makefiles" -DARCH=x64 -DTOOLCHAIN=VS2019 -DTARGET=Release -DCRYPTO=openssl ..
          nmake copy_sample_key
          nmake
      - name: Emu_Test
        run: |
          cd build/bin
          ./spdm_responder_emu &
          sleep 5s
          ./spdm_requester_emu
          sleep 10s
      - name: Responder_validator_Test
        run: |
          cd build/bin
          ./spdm_responder_emu &
          sleep 5s
          ./spdm_device_validator_sample
      - name: Emu_PCIDOE_Test
        run: |
          cd build/bin
          ./spdm_responder_emu --trans PCI_DOE &
          sleep 5s
          ./spdm_requester_emu --trans PCI_DOE
          sleep 10s
